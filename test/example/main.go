package main

// Add takes two integers and returns their sum.
func Add(a, b int) int {
	return a + b
}

func main() {
	// This main function is not strictly necessary for the unit test
	// but is included for completeness as a simple program.
	// You can call Add(x, y) from other packages or directly in tests.
}
